# G0DM0D3 Self-Hosting Stack
#
# Spin up the full stack:
#   1. Copy .env.example to .env and fill in your keys
#   2. docker compose up --build -d
#   3. Open http://localhost:3000
#
# Your friends connect to the same URL — no individual API keys needed.
# The server-side OPENROUTER_API_KEY covers everyone.

services:
  # ── API Server (ULTRAPLINIAN / CONSORTIUM / proxy) ──────
  api:
    platform: "${DOCKER_PLATFORM:-linux/amd64}"
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT=7860
    expose:
      - "7860"
    stop_grace_period: 30s
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:7860/v1/health"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 5

  # ── Frontend (Next.js server) ───────────────────────────
  web:
    platform: "${DOCKER_PLATFORM:-linux/amd64}"
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    environment:
      - PORT=3000
    ports:
      - "${WEB_PORT:-3000}:3000"
    stop_grace_period: 30s
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3000"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 5

  # ── Reverse proxy (nginx) ───────────────────────────────
  proxy:
    platform: "${DOCKER_PLATFORM:-linux/amd64}"
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "${PROXY_PORT:-80}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    stop_grace_period: 30s
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy
